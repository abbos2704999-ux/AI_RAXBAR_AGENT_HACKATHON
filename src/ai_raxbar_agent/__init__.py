"""AI Raxbar Agent -- Batch 1 deterministic, public-safe foundation.

This package contains only offline, synthetic-data, rule-based logic:
typed models, a deterministic risk engine, read-only + one controlled
write tool, and a deterministic policy gate. No LLM, no Google ADK, no
Google Cloud, and no network calls are used anywhere in this package.
"""

__version__ = "0.1.0"
